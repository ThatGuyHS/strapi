'use strict';

/**
 * tjansterbeskrivning router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::tjansterbeskrivning.tjansterbeskrivning');
