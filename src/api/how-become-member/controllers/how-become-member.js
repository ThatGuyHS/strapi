'use strict';

/**
 * how-become-member controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::how-become-member.how-become-member');
